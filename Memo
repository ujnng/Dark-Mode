<input type="button" value="버튼이름" onclick="alert('Dark Mode On')">
onclick="JS" type="button"일때
onchange="" type="text"일때
---javascript keydown event attribute---검색 (event라고 함)

console (자바스트립트 바로 처리 가능)
.length 글자수
Esc 클릭 (console 밑에 보여줌)

javascript data type
undefined : typeof instance === "undefined"
Boolean : typeof instance === "boolean" true/false
Number : typeof instance === "number" 숫자
String : typeof instance === "string" 문자
BigInt : typeof instance === "bigint"
Symbol : typeof instance === "symbol" 

javascript string 검색
.indexof()
"1"문자열
1 숫자열

변수 문자 변환
var name = 'a';
"+name+"

javascript select tag by css selector
document.querySelector(selectors);

javascript element style
document.getelementbyid("").style.

Comparision Operator & Boolean
document.write (1===1);
1===1 왼쪽과 오른쪽 값 같냐? true/false > Boolean이라고 함
&lt; (less than 1>2)

Conditional statement
if(true/false) {

} else {
    
}